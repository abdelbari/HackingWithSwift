{
  "name": "canvia-verify",
  "private": true,
  "description": "Static checks for the Canvia Swift sources (no Xcode required)",
  "scripts": { "verify": "node verify.js ../Canvia ../Tests" },
  "dependencies": { "tree-sitter": "^0.21.0", "tree-sitter-swift": "^0.5.0" }
}
